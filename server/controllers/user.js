//const { users } = require('./temp data/user.js')

const  getUser = (req, res) => {
   res.send("users")
}
const  getDash = (req, res) => {
   res.send("users")
}

module.exports = {getDash, getUser}